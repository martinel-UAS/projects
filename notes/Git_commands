/* Initialize Git environment
git init

/* Git status check the current status
git status

/*Log
git log
git log --oneline

/* Add
git add . (filename) 

/* Restore
git restore --staged filename

/* Delete
git rm filename

/* Rename
git mv filename newfilename
git commit -m  "First Commit"


/*Differences
git diff


rebase

git switch [branch]

GITHUB
git remote add origin https://github.com/martinel-UAS/assignments.git

/* Change remote
git remote set-url origin https://github.com/martinel-UAS/assignments.git

/* Remove remote
git remote remove https://github.com/martinel-UAS/test.git



git push --all

git push -u origin main

/* View remotes
git remote -v